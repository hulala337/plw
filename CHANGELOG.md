# Changelog

## V3.2.5 · 输入采集与多显示器修复

- **修复键盘统计不增长的根因**：旧版本在键盘回调中写入不存在的 `daily.activity_events` 字段，SQLite 抛错后可能直接终止 `pynput` 键盘监听线程，因此键盘与鼠标数据都可能停止更新。
- **输入回调改为内存批量入库**：键盘、点击、滚轮、鼠标移动先进入轻量事件缓冲，再由 tracker 每秒写入 SQLite，避免高频鼠标移动频繁打开数据库导致监听线程被拖慢或异常。
- **键盘监听异常隔离**：回调自身增加异常保护，不让单个事件异常杀死全局键盘 Hook。
- **鼠标移动计入活动状态**：有效移动会刷新活动时间，并累计虚拟桌面坐标下的移动像素。
- **自动识别显示器布局**：Windows 下通过系统显示器枚举识别单屏/多屏，并支持左侧、右侧、上方等非主屏负坐标布局。
- **跨屏统计**：识别鼠标从一个显示器进入另一个显示器，记录“跨屏次数”；移动距离使用 Windows 虚拟桌面坐标连续计算。
- **移除不科学的 km 显示**：界面改为 `px`（屏幕坐标像素），导出字段也明确为“鼠标移动(px)”。像素是可直接验证的测量单位，不把屏幕像素强行换算成物理公里。
- **新增健康诊断**：健康接口分别报告键盘监听、鼠标监听、tracker、数据库状态及最近一次键盘/鼠标事件时间。
- **旧数据库自动迁移**：自动增加 `activity_events` 与 `monitor_switches` 字段，不丢失已有统计。

> 注：本版本仍需在实际 Windows 环境运行一次，才能验证 `pynput` 全局 Hook 与具体多显示器驱动/DPI 组合。源码已通过 Python 语法检查。
