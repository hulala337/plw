# 鹈鹕工作台 V3.2 安全发布指南

## 目标

这版重点保护“TF7Z-XY”版权信息、微信收款码和前端 UI 不被普通二次打包者直接替换。

### 第一层：Nuitka 原生编译

正式发布默认用 Nuitka `onefile` 编译 Python。不会像普通 PyInstaller 发布那样把 `.pyc` 运行代码以容易提取的形式直接摆在发布目录里。

### 第二层：Ed25519 资源签名

构建时自动生成或复用本机私钥，并给以下文件生成签名清单：

- `VERSION.txt`
- `web/index.html`
- `web/assets/app.js`
- `web/assets/style.css`
- `web/assets/wechat_qr.png`

验证公钥被写进编译代码；私钥不会进入安装包。程序启动时先验签，再核对 SHA-256。如果别人只替换收款码、改作者名或改前端页面，验证会失败并拒绝启动。

### 第三层：Windows Authenticode

正式公开分发时，建议购买代码签名证书。构建脚本支持以下环境变量：

`SIGNTOOL_PATH`
`PFX_PATH`
`PFX_PASSWORD`
`TIMESTAMP_URL`（可选）

配置后脚本会签名最终 EXE 和安装器。这样用户可以在 Windows 文件属性的“数字签名”页验证发布者；二进制被修改后原签名会失效。

## 私钥管理

默认路径：

`%USERPROFILE%\\.pelican_workbench\\signing\\private.key`

请备份到密码管理器或离线安全存储，并且绝不把它发给别人、上传 GitHub 或放进源码 ZIP。

## 重要现实边界

没有任何完全离线的 Windows 客户端能做到“绝对不可逆向”。熟练逆向人员可以调试、patch 程序或重新实现客户端。

这套方案解决的是：

1. 普通用户直接解包修改 UI / 收款码；
2. 低成本二次传播者改版权后重新打包；
3. 用户无法区分正版与被修改版时的信任问题。

如果未来需要更高等级的品牌保护，可以增加在线版本验证：客户端周期性请求你的服务器获取“正版版本清单”，服务端返回签名数据；但这会改变完全离线的产品定位，建议作为可选的“联网保护模式”。
